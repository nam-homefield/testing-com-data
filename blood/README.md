# Testing.com blood-testing GEO performance

Auto-published by the `refresh-blood-looker` skill in the testing-com repo, via
`shared/pages-publish/publish_report.sh`.

`index.html` is the team's permanent URL and must never move. Dated snapshots
accumulate under `archive/`. Both are generated: do not hand-edit them, the next
refresh overwrites them.

Served with `noindex,nofollow`. Google Search Console figures for /blood-testing/ pages. Contains no patient information.

Previously published to gist `345f1f29802d622175ec5fc9c02d03a0`; that URL now redirects here. Dated snapshots
from before the move are still in the gist.
