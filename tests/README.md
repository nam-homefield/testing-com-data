# Testing.com individual test pages tracker

Auto-published by the `refresh-tests-dashboard` skill in the testing-com repo, via
`shared/pages-publish/publish_report.sh`.

`index.html` is the team's permanent URL and must never move. Dated snapshots
accumulate under `archive/`. Both are generated: do not hand-edit them, the next
refresh overwrites them.

Served with `noindex,nofollow`. Google Search Console figures for the /tests/, panel and treatment upload cohort. Contains no patient information.

Previously published to gist `fcccd0af96b9c7e4676b7e35f3d1ed98`; that URL now redirects here. Dated snapshots
from before the move are still in the gist.
