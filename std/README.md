# Testing.com STD GEO performance

Auto-published by the `refresh-looker` skill (launchd, Mondays 6:00 local) in the testing-com repo, via
`shared/pages-publish/publish_report.sh`.

`index.html` is the team's permanent URL and must never move. Dated snapshots
accumulate under `archive/`. Both are generated: do not hand-edit them, the next
refresh overwrites them.

Served with `noindex,nofollow`. Google Search Console figures for /std-testing/ pages. Contains no patient information.

Previously published to gist `47593b83aeaa628138f98de2e5977c50`; that URL now redirects here. Dated snapshots
from before the move are still in the gist.
