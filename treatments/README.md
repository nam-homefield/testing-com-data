# Testing.com treatments guides tracker

Auto-published by the `refresh-treatments-dashboard` skill in the testing-com repo, via
`shared/pages-publish/publish_report.sh`.

`index.html` is the team's permanent URL and must never move. Dated snapshots
accumulate under `archive/`. Both are generated: do not hand-edit them, the next
refresh overwrites them.

Served with `noindex,nofollow`. Google Search Console figures for /treatments/ pages. Contains no patient information.

Previously published to gist `61ee29732e700af287a882a18e7bc9bc`; that URL now redirects here. Dated snapshots
from before the move are still in the gist.
